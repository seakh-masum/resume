---
import SkillDetails from "../modals/SkillDetails.astro";
import CircleProgress from "./CircleProgress.astro";
import Modal from "./Modal.astro";

interface Props {
  features: "skills" | "tools";
  data?: any[];
}

const { features, data } = Astro.props;
---

<div class="circle-progress-grid">
  {
    data?.map((item, index) => (
      <div class="grid-item" style={`animation-delay: ${0.1 + index * 0.08}s`}>
        <Modal id={item._key} type="project">
          <CircleProgress slot="trigger" data={item} />
          <SkillDetails slot="content" data={item} feature={features} />
        </Modal>
      </div>
    ))
  }
</div>

<style>
  .circle-progress-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.5rem;
    padding-top: 0.25rem;
  }
  @media (min-width: 640px) {
    .circle-progress-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  .grid-item {
    opacity: 0;
    transform: scale(0.85) translateY(12px);
    animation: grid-item-enter 0.35s ease-out forwards;
  }
  @keyframes grid-item-enter {
    to {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }
</style>
