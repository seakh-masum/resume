---
interface Props {
  text: string;
  className?: string;
}

const { text, className = "" } = Astro.props as Props;
---

<h2
  class={`m-0 mb-[6mm] text-[5.2mm] tracking-[0.35em] uppercase font-bold text-print-ink ${className}`.trim()}
>
  {text}
</h2>
