import { sanity } from "@/lib/sanity";
import { RESUME_QUERY } from "@/lib/queries";

const resume = await sanity.fetch(RESUME_QUERY);

<main class="container mx-auto p-6">
  <h1 class="text-3xl font-bold">{resume.profile.name}</h1>
</main>
